// TASK 2: initial mount render
// Turns a VNode into real DOM and appends it to container.

import type { VNode } from "./types";

export function render(vnode: VNode | string | null, container: HTMLElement) {
  // TODO: mount implementation (no diffing yet)
}
